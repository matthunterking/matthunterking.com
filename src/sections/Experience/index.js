import React from 'react';

import Section from '../../containers/Section';

const Experience = ({ backgroundColor }) => (
 <Section backgroundColor={backgroundColor}>
  <h2>Experiance</h2>
 </Section>
);

export default Experience;
